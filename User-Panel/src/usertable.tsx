import { useReactTable } from '@tanstack/react-table'

function UserTable(){
    const table = useReactTable(options)
    return(
        <div>
            
        </div>
    );
}

export { UserTable};