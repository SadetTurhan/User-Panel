import { useReactTable } from '@tanstack/react-table'

function UserTable(){
    const table = useReactTable(Option)
    return(
        <div>
            
        </div>
    );
}

export { UserTable};